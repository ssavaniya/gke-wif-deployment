apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: hello-gke

spec:
  rules:

  - http:

      paths:

      - path: /

        pathType: Prefix

        backend:

          service:
            name: hello-gke-ingress

            port:
              number: 80
