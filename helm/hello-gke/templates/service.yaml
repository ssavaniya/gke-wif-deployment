apiVersion: v1
kind: Service

metadata:
  name: hello-gke
  labels:
    app: hello-gke

spec:
  type: {{ .Values.service.type }}

  selector:
    app: hello-gke

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
