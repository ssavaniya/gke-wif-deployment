apiVersion: v1
kind: Service

metadata:
  name: hello-gke

spec:
  type: {{ .Values.service.type }}

  selector:
    app: hello-gke

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
